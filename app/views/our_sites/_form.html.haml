= semantic_form_for our_site do |f|
  = f.inputs do
    = f.input :name
    = f.input :category, :as => :select
  = f.buttons :class => "form-actions" do
    = f.commit_button :button_html => { :class => 'btn btn-primary', :disable_with => t("views.application.wait") }

