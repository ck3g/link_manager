= simple_form_for seller_origin, html: { class: "form-horizontal" } do |f|
  = f.input :name
  .form-actions
    = f.button :submit, class: "btn btn-primary", disable_with: t("views.application.wait")
